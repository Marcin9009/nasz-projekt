# is_ready = True
# is_runing = False

# result_1 = is_ready and is_runing
# result_2 = is_ready or is_runing
# result_3 = is_ready ^ is_runing
# result_4 = not is_ready
# result_5 = (is_ready == True) and (is_runing == False)
g

(is_runing == False) # parenthese are optional; added for readability
